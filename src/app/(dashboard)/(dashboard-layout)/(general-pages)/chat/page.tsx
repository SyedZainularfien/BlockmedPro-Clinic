import React from 'react';

import DashboardWrapper from '@/components/shared/dashboard-wrapper';
import ComingSoon from '@/components/ui/coming-soon';

const Chat = () => {
  return (
    <DashboardWrapper title="Chat" subTitle="Start conversation with others, with ease">
      <ComingSoon />
    </DashboardWrapper>
  );
};

export default Chat;
